apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-om
  labels:
    app.kubernetes.io/name: ozone
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: ozone-manager
spec:
  serviceName: {{ .Release.Name }}-om
  replicas: {{ .Values.ozone.om.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: ozone
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: om
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ozone
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: om
    spec:
      initContainers:
        - name: wait-scm
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z {{ .Release.Name }}-scm-0.{{ .Release.Name }}-scm.{{ .Release.Namespace }}.svc.cluster.local 9860; do echo waiting for SCM; sleep 5; done']
      containers:
        - name: om
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/bin/bash", "-c"]
          args:
            - |
              if [ ! -f /data/metadata/om/current/VERSION ]; then
                echo "OM not initialized — running ozone om --init ..."
                /opt/hadoop/bin/ozone om --init 2>&1
                echo "OM init exit code: $?"
              else
                echo "OM already initialized."
              fi
              echo "Starting OM..."
              exec /opt/hadoop/bin/ozone om
          ports:
            - name: rpc
              containerPort: 9862
            - name: http
              containerPort: 9874
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /opt/hadoop/etc/hadoop/ozone-site.xml
              subPath: ozone-site.xml
            - name: config
              mountPath: /opt/hadoop/etc/hadoop/log4j.properties
              subPath: log4j.properties
          resources:
            {{- toYaml .Values.resources.om | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /
              port: 9874
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: {{ .Release.Name }}-config
        {{- if ne .Values.persistence.type "pvc" }}
        - name: data
          emptyDir: {}
        {{- end }}
  {{- if eq .Values.persistence.type "pvc" }}
  # Sibling of `template`, NOT inside template.spec. Indented at 6 spaces it
  # landed in spec.template.spec, where Kubernetes silently discards it as an
  # unknown field -- no PVC was ever created, and the container's
  # `volumeMounts: - name: data` then failed with `Not found: "data"`, taking
  # the whole install down. Only the pvc path was affected; the ephemeral
  # branch above supplies its own emptyDir named `data`, which is why this
  # went unnoticed and why Ozone could only ever run without persistence.
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- with .Values.persistence.storageClass }}
        storageClassName: {{ . }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.om.size }}
  {{- end }}
