# BDP UI — Next.js standalone output only (no source, no node_modules dev deps).
FROM node:20-alpine
RUN addgroup -S bdp && adduser -S -G bdp -u 10001 bdp
WORKDIR /app
COPY --chown=10001:10001 dist/ui/ .
USER 10001
ENV PORT=3000 HOSTNAME=0.0.0.0
EXPOSE 3000
CMD ["node", "server.js"]
